import { Level } from '../../components/screens/Level';

// TODO desktop view — no Antre/Antri Pedagang Desktop.dc.html reference for
// this screen yet; falls back to the mobile view (D0 rule), centered by
// AppShell's max-width column, until the desktop design lands.
export default function LevelPage() {
  return <Level />;
}
