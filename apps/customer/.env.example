# Data layer mode: `mock` (default, runs with no backend) or `http` (Elysia).
NEXT_PUBLIC_API_MODE=mock
# Base URL of the Elysia backend when NEXT_PUBLIC_API_MODE=http.
NEXT_PUBLIC_API_URL=http://localhost:3333
